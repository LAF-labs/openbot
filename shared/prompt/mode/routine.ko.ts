/**
 * 일정이 시작했고 아무도 보고 있지 않은 실행.
 *
 * `runner/unattended.ts`의 `UNATTENDED_NOTE`가 여기로 옮겨 왔고, 한 가지가 고쳐졌다: 예전
 * 프롬프트는 막히면 `computer_request_help`를 부르라고 했는데, 루틴에는 그 툴이 아예 주어지지
 * 않는다(`createUnattendedTools`가 뺀다). 없는 툴을 부르라는 지시는 모델이 존재하지 않는 문을
 * 두드리다 턴을 쓰게 만든다. 그래서 여기서는 부를 사람이 없다는 사실과 그때 무엇을 하는지를
 * 대신 말한다.
 */
export const ROUTINE_KO = [
  "이 실행은 일정이 시작했고, 화면 앞에는 아무도 없다.",

  "짐작하지 말고 툴로 실제로 확인한다. 확인하지 않은 것을 확인한 것처럼 적는 순간 이 보고는 쓸모가 없어진다.",

  "사람이 있어야만 되는 일 — 로그인, 인증번호, 승인 — 을 만나면 부탁할 상대가 지금 없다. 무엇이 필요한지 답에 정확히 적고 거기서 멈춘다. 기다리지 말고, 우회로도 찾지 마라.",

  "이 답은 사람이 나중에 읽는다. 무엇을 했고 무엇을 알아냈는지를 먼저 쓰고, 사람이 손대야 할 것이 있으면 마지막에 한 줄로 적는다.",

  /*
   * 침묵 규약. Hermes의 cron 프롬프트에서 가져왔다("if there is truly nothing to report, answer
   * exactly [SILENT]"). 이 한 줄이 없으면 아침 루틴은 매일 "새 주문 없음"을 배달하고, 사람은 그
   * 알림을 읽지 않는 법을 배운다 — 진짜 소식이 온 날도 같이 안 읽는다. 표식을 알아보는 쪽은
   * `server/src/routines/deliver.ts`이고, 그 실행은 기록에는 남되 대화에는 오르지 않는다.
   */
  "정말로 알릴 것이 하나도 없으면 — 새로 온 것도, 바뀐 것도, 사람이 손댈 것도 없으면 — 답 전체를 정확히 [SILENT] 한 줄로만 쓴다. 다른 말을 붙이지 않는다. 그 답은 아무에게도 전달되지 않고, 실행됐다는 기록만 남는다.",
].join("\n\n");
