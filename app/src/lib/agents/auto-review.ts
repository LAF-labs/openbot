/**
 * THREE SENTENCES OF THE RIGHT SHAPE, because the hard part is not being warned.
 *
 * The "묻지 않아도 되는 일" box used to be three paragraphs of caveat — that a model reads it, that
 * the deployment's own rules still win, and that anything let through "was seen by nobody, and is
 * recorded that way in the audit trail", which names a trail an owner has no screen for. All of it
 * true, and the effect of it was a box nobody ever typed in.
 *
 * An example is the shorter version of the same lesson: read one and you can see that this is a
 * sentence about a KIND of action, not a rule language. Tapping one writes it into the box, where
 * it can be edited into somebody's own.
 *
 * Read through `t(variable)`, so `bot-profile.test.ts` walks them for Korean.
 */
export const AUTO_REVIEW_EXAMPLES: readonly string[] = [
  "Reading anything on our own site is fine.",
  "Looking things up is fine. Sending anything is not.",
  "Saving a draft is fine.",
];
